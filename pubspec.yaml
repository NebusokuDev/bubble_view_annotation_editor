name: bubble_view_annotation_editor
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0
dependencies:
  flutter:
    sdk: flutter
  go_router:
  file_picker:
  http:
  path:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:
  sqflite:
  shared_preferences:

flutter:
  uses-material-design: true