name: bubble_view_annotation_editor
description: "image anotation editor"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0
dependencies:
  flutter:
    sdk: flutter
  go_router:
  file_picker:
  font_awesome_flutter:
  shared_preferences:
  http:
  path:
  riverpod:
  flutter_riverpod:
  undo:
  sqlite3:
  sqflite:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints:

flutter:
  uses-material-design: true
  assets:
    - assets/sql/create.sql